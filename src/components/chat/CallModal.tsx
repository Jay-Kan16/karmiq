import {motion} from "framer-motion";import {Phone,X} from "lucide-react";export default function CallModal({phone,name,onClose}:{phone:string;name:string;onClose:()=>void}){return <motion.div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/40 p-4" initial={{opacity:0}} animate={{opacity:1}}><div className="card w-full max-w-sm p-7 text-center"><button onClick={onClose} className="float-right"><X size={18}/></button><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-brand-100 text-brand-700"><Phone size={30}/></div><h2 className="mt-4 text-xl font-black">{name}</h2><p className="mt-1 text-slate-500">{phone}</p><a href={`tel:${phone}`} className="btn-primary mt-6 w-full"><Phone size={17}/> Call worker</a></div></motion.div>}
