import {useEffect,useState} from "react";import {BadgeCheck} from "lucide-react";import {api} from "../../services/api";export default function Workers(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.getAdminWorkers().then(setData)},[]);return <div><h1 className="section-title">Workers</h1><div className="mt-5 grid gap-3">{data.map(w=><div className="card flex items-center justify-between p-4" key={w._id}><div><p className="font-bold">{w.userId?.name}</p><p className="text-sm text-slate-500">{w.skills?.join(", ")} • {w.rating?.toFixed?.(1)||"0"} ⭐</p></div><span className="text-xs font-bold">{w.verificationStatus==="VERIFIED"&&<BadgeCheck className="inline text-brand-600"/>} {w.availability}</span></div>)}</div></div>}