import {useEffect,useState} from "react";import {api} from "../../services/api";export default function Complaints(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.getComplaints().then(setData)},[]);const resolve=async(id:string)=>{await api.setComplaintStatus(id,"RESOLVED");setData(x=>x.map(c=>c._id===id?{...c,status:"RESOLVED"}:c))};return <div><h1 className="section-title">Complaints</h1><div className="mt-5 grid gap-3">{data.map(c=><div key={c._id} className="card p-5"><p className="font-bold">{c.issue}</p><p className="mt-1 text-sm text-slate-500">Customer: {c.customerId?.name}</p><span className="mt-3 inline-block rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{c.status}</span>{c.status!=="RESOLVED"&&<button onClick={()=>resolve(c._id)} className="btn-primary ml-3 px-3 py-2 text-xs">Resolve</button>}</div>)}</div></div>}