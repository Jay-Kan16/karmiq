import {useEffect,useState} from "react";import {BadgeCheck,Banknote,Briefcase,Users,Wallet} from "lucide-react";import KpiCard from "../../components/dashboard/KpiCard";import {api} from "../../services/api";export default function AdminDashboard(){const[a,setA]=useState<any>();useEffect(()=>{api.getAdminAnalytics().then(setA)},[]);if(!a)return <div>Loading dashboard…</div>;return <div><p className="text-xs font-bold uppercase tracking-widest text-brand-600">Cooperative command center</p><h1 className="mt-1 text-3xl font-black">Admin Dashboard</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><KpiCard label="Total Workers" value={String(a.totalWorkers)} icon={Users}/><KpiCard label="Verified Workers" value={String(a.verifiedWorkers)} icon={BadgeCheck}/><KpiCard label="Active Workers" value={String(a.activeWorkers)} icon={Briefcase}/><KpiCard label="Today's Bookings" value={String(a.todaysBookings)} icon={Briefcase}/><KpiCard label="Revenue" value={`₹${a.revenue}`} icon={Banknote}/><KpiCard label="Welfare Contributions" value="—" icon={Wallet}/></div></div>}