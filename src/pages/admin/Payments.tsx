import {useEffect,useState} from "react";import {api} from "../../services/api";export default function Payments(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.getAdminBookings().then(x=>setData(x.filter((b:any)=>b.paymentStatus)))},[]);return <div><h1 className="section-title">Payments</h1><div className="card mt-5 overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="bg-slate-50"><th className="p-4">Booking</th><th>Amount</th><th>Status</th></tr></thead><tbody>{data.map(b=><tr className="border-t" key={b._id}><td className="p-4">{b._id}</td><td>₹{b.fare}</td><td>{b.paymentStatus}</td></tr>)}</tbody></table></div></div>}