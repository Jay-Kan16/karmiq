import {useEffect,useState} from "react";import {api} from "../../services/api";export default function Payments(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.getPayments().then(setData)},[]);return <div><h1 className="section-title">Payments</h1><div className="card mt-5 overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="bg-slate-50"><th className="p-4">Booking</th><th>Amount</th><th>Method</th><th>Status</th></tr></thead><tbody>{data.map(p=><tr key={p._id} className="border-t"><td className="p-4">{p.bookingId}</td><td>₹{p.amount}</td><td>{p.method||"—"}</td><td>{p.status}</td></tr>)}</tbody></table></div></div>}