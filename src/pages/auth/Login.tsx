import {motion} from "framer-motion"; import {ArrowRight,Globe,ShieldCheck,Users} from "lucide-react"; import {Navigate,useNavigate} from "react-router-dom"; import {useState} from "react"; import Logo from "../../components/common/Logo"; import {useApp} from "../../context/AppContext";
export default function Login(){const nav=useNavigate();const{user,loginReal,language,setLanguage,t}=useApp() as any;const[phone,setPhone]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);if(user)return <Navigate to={`/${user.role}`} replace />;const submit=async()=>{setLoading(true);setError("");try{const u=await loginReal(phone,password);nav(`/${u.role}`)}catch(e){setError(e instanceof Error?e.message:"Login failed")}finally{setLoading(false)}};return <div className="relative min-h-screen bg-gradient-to-br from-brand-50 via-white to-slate-50"><div className="absolute right-6 top-6"><button onClick={()=>setLanguage(language==="en"?"hi":"en")} className="flex items-center gap-2 rounded-full border bg-white px-3 py-2 text-xs font-bold"><Globe size={15}/>{language==="hi"?"English":"हिन्दी"}</button></div><div className="mx-auto grid min-h-screen max-w-6xl items-center gap-10 px-5 py-10 lg:grid-cols-2"><div className="hidden lg:block"><span className="rounded-full bg-brand-100 px-3 py-1 text-xs font-bold text-brand-700">{t("coopMarketplace")}</span><h1 className="mt-6 max-w-xl text-5xl font-black">{t("loginHeroTitle")}</h1><p className="mt-5 max-w-lg text-lg text-slate-500">{t("loginHeroSub")}</p><div className="mt-8 grid grid-cols-3 gap-3"><Feature icon={ShieldCheck} text={t("verifiedFeature")}/><Feature icon={Users} text={t("localFeature")}/><Feature icon={ShieldCheck} text={t("fastFeature")}/></div></div><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="card mx-auto w-full max-w-md p-7"><Logo/><p className="mt-8 text-sm text-slate-500">{t("welcomeBack")}</p><h2 className="text-3xl font-black">{t("signInTitle")}</h2><label className="mt-6 block text-sm font-bold">Mobile number</label><input className="input mt-2" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+91 98765 43210" inputMode="tel"/><label className="mt-4 block text-sm font-bold">Password</label><input className="input mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/>{error&&<p className="mt-3 rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button onClick={submit} disabled={!phone||!password||loading} className="btn-primary mt-5 w-full">{loading?"Signing in…":"Sign in"} <ArrowRight size={17}/></button><p className="mt-5 text-center text-sm text-slate-500">New here? <a className="font-bold text-brand-700" href="/register">Create an account</a></p><p className="mt-5 text-center text-xs text-slate-400">{t("termsPolicy")}</p></motion.div></div></div>}
function Feature({icon:Icon,text}:{icon:any;text:string}){return <div className="card p-4"><Icon className="text-brand-600" size={22}/><p className="mt-2 text-sm font-bold">{text}</p></div>}
