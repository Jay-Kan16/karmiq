import "dotenv/config"; import {connectDB} from "../config/db.js"; import Service from "../models/Service.js"; import User from "../models/User.js"; import bcrypt from "bcryptjs";
const services=[["Electrician","⚡",299,"Home electrical repair",true],["Plumber","🔧",249,"Plumbing and leak repair",true],["Cleaner","🧹",199,"Home and deep cleaning",true],["Carpenter","🪚",349,"Carpentry and furniture repair",false],["Painter","🎨",499,"Painting and wall work",false],["Caregiver","❤️",399,"Elderly and home care assistance",true],["Gardener","🌱",249,"Garden and lawn services",false],["Technician","🔧",299,"Appliance and electronics repair",true]].map(([name,icon,startingPrice,description,emergency])=>({name,icon,startingPrice,description,emergency,category:"Household",active:true}));
await connectDB(); for(const s of services)await Service.updateOne({name:s.name},{$setOnInsert:s},{upsert:true}); if(process.env.ADMIN_PHONE&&process.env.ADMIN_PASSWORD&&process.env.ADMIN_NAME){const password=await bcrypt.hash(process.env.ADMIN_PASSWORD,12);await User.updateOne({phone:process.env.ADMIN_PHONE},{$setOnInsert:{name:process.env.ADMIN_NAME,phone:process.env.ADMIN_PHONE,password,role:"admin"}},{upsert:true});console.log("Admin account provisioned from environment.");} console.log("Services seeded (explicit command only)"); process.exit(0);